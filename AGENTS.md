# Agents

## Python environment

Always use the project venv when running Python commands:

```bash
.venv/bin/python main.py ...
```

Do NOT use bare `python` or `python3` — they may point to the system Python which lacks project dependencies.
